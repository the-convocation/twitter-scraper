services:
  twitter-scraper:
    build: .
    env_file:
      - .env.local
    environment:
      - DATABASE_URL=postgresql://neondb_owner:npg_hNJyn57adrgx@ep-aged-truth-a1vexyjq-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
      - TARGET_ACCOUNTS=elonmusk,NASA
      - KEYWORDS=mars,crypto
    # Or specify individual environment variables:
    # environment:
    #   - TWITTER_USERNAME=${TWITTER_USERNAME}
    #   - TWITTER_PASSWORD=${TWITTER_PASSWORD}
    #   - TWITTER_EMAIL=${TWITTER_EMAIL}
    #   - TWITTER_COOKIES=${TWITTER_COOKIES}
    #   - PROXY_URL=${PROXY_URL}